services:
  borg-ui:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "5173:5173"
    command: sh -c "yarn install && yarn dev --host 0.0.0.0 --port 52134" 